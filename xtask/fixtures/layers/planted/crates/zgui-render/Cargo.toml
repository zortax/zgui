[package]
name = "zgui-render"
version = "0.1.0"
edition = "2024"

# L1 — contracts. Every dependency is inherited: `foo.workspace = true`.
[dependencies]
zgui-geom = { path = "../zgui-geom", version = "0.1.0" }
# PLANTED VIOLATION: a contract may not name the backend that implements it.
zgui-render-wgpu = { path = "../zgui-render-wgpu", version = "0.1.0" }

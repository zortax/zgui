[package]
name = "zgui-image"
version.workspace = true
edition.workspace = true
license.workspace = true

# L1 — contracts and pure data. Every dependency is inherited: `foo.workspace = true`.
[dependencies]
# The device-pixel vocabulary a decoded extent is reported in.
zgui-geom = { path = "../zgui-geom", version = "0.1.0" }

# The codecs. This crate is the only one in the workspace that names them, so which formats an
# application links is decided in exactly one place.
image.workspace = true
thiserror.workspace = true

[lints]
workspace = true

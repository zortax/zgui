//! The two flex-container properties that are not shared with any other layout mode.

/// The computed value of `flex-direction`.
pub use style::computed_values::flex_direction::T as FlexDirectionValue;
/// The computed value of `flex-wrap`.
pub use style::computed_values::flex_wrap::T as FlexWrapValue;

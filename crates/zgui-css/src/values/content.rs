//! Generated content: what a `::before` or `::after` box is made of.

/// The computed value of `content`: `normal`, `none`, or a list of items.
pub use style::values::computed::Content as ContentValue;
/// One item of a `content` list — a literal string, a counter, a quote, an attribute or an image.
pub use style::values::computed::ContentItem as ContentItemValue;
/// The item list of a `content` value, with the index at which its alternative text begins.
pub use style::values::generics::counters::GenericContentItems as ContentItemsValue;

[package]
name = "zgui-color"
version.workspace = true
edition.workspace = true
license.workspace = true

# L0 — foundation. Every dependency is inherited: `foo.workspace = true`.
[dependencies]

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true

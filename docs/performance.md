# Performance

Generated by `cargo xtask perf`, which is a step of `cargo xtask ci`. Every number below was measured by the run that wrote this file; nothing here is typed in by hand, and a number that moved moved this document with it.

Each measurement carries a **band**. A time band is a baseline and a tolerance, because a duration is a property of the machine as much as of the code; a count band is a ceiling with no tolerance at all, because a count is a property of the design and reads the same on a slow machine, a fast one and under a debugger. A run outside any band fails the gate.

Stored runs: 68, under `docs/perf/runs/`, one tab-separated file each.

## idle

| measurement | value | unit | distribution | band | verdict | budget | why the band is this wide |
|---|---:|---|---|---:|---|---|---|
| `idle.frames` | 0.00 | frames | a count, which has no tail | 0.00 | ok | 0.00 met | a still document draws nothing; one frame here is the whole defect |
| `idle.turn` | 0.07 | us | p50=0.0690 p95=0.0700 p99=0.0700 max=0.2180 n=600 | 2.00 | ok | — | a turn that draws nothing touches no node; measured at well under 1 us |
| `reshape.paragraphs_forgotten` | 0.00 | paragraphs | a count, which has no tail | 0.00 | ok | — | one element's brush moving costs that element's paragraphs, not the window's |
| `reshape.boxes_marked_all_dirty` | 0.00 | boxes | a count, which has no tail | 0.00 | ok | — | nothing these scenarios drive changes the device scale, which is the one change every box pays for |

Frames delivered: **0 of 600 were late** (0.00 %), against the 16.667 ms interval this scenario drove them at. A frame is late when it took more than one and a half of that interval, whatever it cost inside itself.


## hover-storm

| measurement | value | unit | distribution | band | verdict | budget | why the band is this wide |
|---|---:|---|---|---:|---|---|---|
| `hover.crossing` | 161.00 | us | p50=160.9990 p95=177.8600 p99=254.0500 max=1117.3840 n=240 | 266.00 | ok | 500.00 met | measured at 187 us, well under the half-millisecond budget |
| `hover.primitives_emitted` | 41.56 | prims | a count, which has no tail | 200.00 | ok | 200.00 met | two rows changed, so a frame that emits the table is the defect |
| `reshape.paragraphs_forgotten` | 0.00 | paragraphs | a count, which has no tail | 0.00 | ok | — | one element's brush moving costs that element's paragraphs, not the window's |
| `reshape.boxes_marked_all_dirty` | 0.00 | boxes | a count, which has no tail | 0.00 | ok | — | nothing these scenarios drive changes the device scale, which is the one change every box pays for |

Frames delivered: **0 of 240 were late** (0.00 %), against the 8.333 ms interval this scenario drove them at. A frame is late when it took more than one and a half of that interval, whatever it cost inside itself.


## scroll

| measurement | value | unit | distribution | band | verdict | budget | why the band is this wide |
|---|---:|---|---|---:|---|---|---|
| `scroll.translation` | 246.89 | us | p50=246.8940 p95=293.5840 p99=312.5020 max=334.0180 n=236 | 350.00 | ok | 1000.00 met | measured at 217-250 us across runs; the band sits under the budget |
| `scroll.recycle` | 900.65 | us | p50=900.6480 p95=1183.5930 p99=1420.4820 max=1517.0440 n=114 | 1232.00 | ok | 1500.00 met | measured at 872-892 us across runs; the band sits under the budget |
| `scroll.translation.restyles` | 0.00 | elems | a count, which has no tail | 0.00 | ok | 0.00 met | moving content changes no computed style |
| `scroll.translation.relayouts` | 0.00 | nodes | a count, which has no tail | 0.00 | ok | 0.00 met | moving content changes no size |
| `scroll.translation.hit_rebuilds` | 0.00 | rebuilds | a count, which has no tail | 0.00 | ok | 0.00 met | the hit index is translated with the content, never rebuilt from it |
| `reshape.paragraphs_forgotten` | 0.00 | paragraphs | a count, which has no tail | 0.00 | ok | — | one element's brush moving costs that element's paragraphs, not the window's |
| `reshape.boxes_marked_all_dirty` | 0.00 | boxes | a count, which has no tail | 0.00 | ok | — | nothing these scenarios drive changes the device scale, which is the one change every box pays for |

Frames delivered: **0 of 350 were late** (0.00 %), against the 8.333 ms interval this scenario drove them at. A frame is late when it took more than one and a half of that interval, whatever it cost inside itself.


## cold-start

| measurement | value | unit | distribution | band | verdict | budget | why the band is this wide |
|---|---:|---|---|---:|---|---|---|
| `cold.first_frame` | 116.86 | ms | p50=116.8552 p95=116.8552 p99=116.8552 max=116.8552 n=1 | 140.00 | ok | 250.00 met | measured at 102-111 ms headless; on screen with a device it is 216-247 ms |
| `reshape.paragraphs_forgotten` | 0.00 | paragraphs | a count, which has no tail | 0.00 | ok | — | one element's brush moving costs that element's paragraphs, not the window's |
| `reshape.boxes_marked_all_dirty` | 0.00 | boxes | a count, which has no tail | 0.00 | ok | — | nothing these scenarios drive changes the device scale, which is the one change every box pays for |

Frames delivered: **1 of 1 were late** (100.00 %), against the 0.017 ms interval this scenario drove them at. A frame is late when it took more than one and a half of that interval, whatever it cost inside itself.


## kitchen-sink

| measurement | value | unit | distribution | band | verdict | budget | why the band is this wide |
|---|---:|---|---|---:|---|---|---|
| `kitchen.resize` | 6.20 | ms | p50=6.1972 p95=8.4100 p99=8.7093 max=8.7093 n=32 | 10.25 | ok | — | the measured on-screen p50, which every box in the window pays into |
| `kitchen.click` | 28.33 | us | p50=28.3310 p95=29.5980 p99=152.0360 max=152.0360 n=32 | 15.12 | **regressed** | — | the measured p50 at 1 851 boxes: one class on one element |
| `kitchen.keystroke` | 632.47 | us | p50=632.4720 p95=727.3370 p99=894.2610 max=894.2610 n=32 | 407.40 | **regressed** | — | the measured p50: one edit, one paragraph reshaped, one box repainted |
| `kitchen.untouched_turn` | 51.72 | us | p50=51.7220 p95=76.7180 p99=84.6290 max=109.6990 n=240 | 30.80 | **regressed** | — | the measured p50 of one turn over an untouched but animating gallery |
| `kitchen.glide_tick` | 876.59 | us | p50=876.5940 p95=1037.6170 p99=1068.9000 max=1365.6480 n=120 | 854.00 | **regressed** | — | the measured p50 of one frame of the framework's own scroll animation |
| `kitchen.tab_traversal` | 377.08 | us | p50=377.0770 p95=432.3180 p99=502.1670 max=502.1670 n=22 | 533.40 | ok | — | one Tab press moves focus by one stop, which is one element's style and one element's shaping |
| `reshape.paragraphs_forgotten` | 0.00 | paragraphs | a count, which has no tail | 0.00 | ok | — | one element's brush moving costs that element's paragraphs, not the window's |
| `reshape.boxes_marked_all_dirty` | 0.00 | boxes | a count, which has no tail | 0.00 | ok | — | nothing these scenarios drive changes the device scale, which is the one change every box pays for |

Frames delivered: **0 of 478 were late** (0.00 %), against the 16.667 ms interval this scenario drove them at. A frame is late when it took more than one and a half of that interval, whatever it cost inside itself.


## Budgets

Every measurement that has a budget met it. A budget is what the design was supposed to cost and does not move with the measurement, so this is the statement a band cannot make: nothing here is merely no worse than it was.

## Where the frame's cost is

The three counters below are what decides whether the scene rebuild is the dominant cost of a frame, which is the question a retention scheme would be the answer to. They are recorded for every scenario rather than for the one somebody suspected, because a number that exists only where it was expected proves nothing about anywhere else.

| scenario | primitives emitted | primitives culled | bounds-tree inserts |
|---|---:|---:|---:|
| idle | 0 | 0 | 0 |
| hover-storm | 9975 | 241916 | 9975 |
| scroll | 121020 | 29242 | 121020 |
| cold-start | 833 | 5350 | 833 |
| kitchen-sink | 123548 | 207759 | 123944 |

And what each scenario found that no band expresses — a band says whether something got worse, and these say what the frame is actually doing:

- **scroll** — one row shift rebuilds 8 boxes and relays out 29 nodes, which is the row that arrived and not the port it arrived in; it still emits 405 primitives, which is the port, and is what a recycle frame's remaining cost is

- **cold-start** — this distribution is taken over a population of one, and it has to be: the second start in a process is a warm start wearing this scenario's name, so there is no second sample to take. Read the four figures as the one launch they are

- **kitchen-sink** — the tab traversal is inside the measured part now, and it is the widest distribution this scenario takes: p50 377.1 us, p99 502.2 us, max 502.2 us over 22 presses
